% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_n_covr.R
\name{check_n_covr}
\alias{check_n_covr}
\title{Run R CMD check and code coverage in one pass}
\usage{
check_n_covr(pkg = ".", args = character(0), quiet = TRUE)
}
\arguments{
\item{pkg}{Path to the package.}

\item{args}{Character vector of extra args passed to
\code{devtools::check()}. \code{--no-tests} is always prepended; pass
\code{--as-cran}, \code{--no-manual} etc. here.}

\item{quiet}{Logical. Forwarded to both inner runners
(\code{devtools::check(quiet = ...)} and
\code{covr::package_coverage(quiet = ...)}). Default \code{TRUE}.}
}
\value{
A named list with two elements:
\itemize{
\item \code{check}: the \code{rcmdcheck} result returned by
\code{devtools::check()}.
\item \code{coverage}: the \code{package_coverage} object returned by
\code{covr::package_coverage()}.
}
}
\description{
Splits the work between \code{devtools::check(args = "--no-tests")}
(the static / install / vignette parts of \verb{R CMD check}) and
\code{covr::package_coverage(type = "tests")} (the test runner, with
coverage instrumentation). The unit tests run exactly once, on
the coverage side, instead of twice (once for the check, once
for the coverage). On a package with a slow suite this halves
the wait.
}
\examples{
\dontrun{
res <- check_n_covr(".")
res$check
covr::percent_coverage(res$coverage)
}
}
