% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/asciify_pkg.R
\name{find_nonascii_files}
\alias{find_nonascii_files}
\title{Scan a directory tree for files containing non-ASCII characters}
\usage{
find_nonascii_files(
  path = ".",
  scope = c("R", "tests", "vignettes", "man", "DESCRIPTION", "NAMESPACE"),
  ignore_ext = c("png", "jpg", "jpeg", "gif", "rds", "rda", "rdata", "pdf", "ico", "svg"),
  size_limit = 5e+05
)
}
\arguments{
\item{path}{directory to scan, recursively.}

\item{scope}{subdirectories, relative to \code{path}, to consider. Defaults to
the CRAN-relevant ones (\code{R}, \code{tests}, \code{vignettes}, \code{man}, plus the
top-level \code{DESCRIPTION} / \code{NAMESPACE}).}

\item{ignore_ext}{file extensions (lowercase, no dot) that are treated as
binary and skipped without reading.}

\item{size_limit}{only files smaller than this many bytes are scanned;
safety net for accidentally bundled large blobs.}
}
\value{
a data.frame with columns \code{file}, \code{line}, \code{text}, \code{n_tokens}
(count of non-ASCII characters on the offending line; well-defined
for both R and non-R files), sorted by file then line. Empty if
nothing is found.
}
\description{
Lightweight cousin of \code{dreamRs/prefixer::show_nonascii_file()} - returns a
tidy data.frame instead of pushing markers to the RStudio editor.
}
