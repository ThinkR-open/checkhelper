% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit_dontrun.R
\name{audit_dontrun}
\alias{audit_dontrun}
\title{Audit \verb{\dontrun{}} blocks across the package's Rd files}
\usage{
audit_dontrun(pkg = ".")
}
\arguments{
\item{pkg}{Path to the package to audit.}
}
\value{
A tibble with columns \code{rd_file}, \code{topic}, \code{line},
\code{suggestion}. Empty when the package has no \verb{\dontrun{}}
blocks, or when there is no \verb{man/} directory.
}
\description{
Surfaces every \verb{\dontrun{}} block in \verb{man/*.Rd}, with the source
Rd file, the documented topic, the line number, and a one-line
suggestion of the modern equivalent. CRAN policy is that
\verb{\dontrun{}} should only wrap example code that genuinely cannot
be executed (missing API key, missing system dependency, side
effect on the user's filespace). The contributor should otherwise
use \verb{\donttest{}}, which still gets exercised by
\verb{R CMD check --run-donttest} but is skipped by default.
}
\details{
Detection is purely static: each Rd file is read line-by-line and
the literal \verb{\dontrun\{} opener is matched (commented-out forms
starting with \verb{\%} are ignored). The function never sources the
file and never executes user code.
}
\examples{
\dontrun{
audit_dontrun(".")
}
}
