% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/asciify_helpers.R
\name{asciify_r_source}
\alias{asciify_r_source}
\title{Rewrite non-ASCII characters inside a string of R source code}
\usage{
asciify_r_source(
  text,
  strategy = c("auto", "escape", "translit", "report"),
  identifiers = c("error", "warn", "skip")
)
}
\arguments{
\item{text}{character(1), the full source of an R script.}

\item{strategy}{one of:
\itemize{
\item \code{"auto"} (default): per-token policy - \verb{\\\\uXXXX} escape inside
string literals (so they remain semantically equivalent and CRAN-safe);
\code{Latin-ASCII} transliteration (drops diacritics, e.g. an accented
\code{e} becomes plain \code{e}) inside comments and roxygen blocks (where
escapes would not be interpreted).
\item \code{"escape"}: force \verb{\\\\uXXXX} escape on every non-identifier token.
\item \code{"translit"}: force ASCII transliteration on every non-identifier token.
\item \code{"report"}: rewrite nothing, just return the input unchanged. Useful
in conjunction with \code{\link[=find_nonascii_tokens]{find_nonascii_tokens()}} for a dry run.
}}

\item{identifiers}{what to do when a non-ASCII identifier (variable,
function name, formal, slot...) is found:
\itemize{
\item \code{"error"} (default): stop. Renaming an identifier changes the API
surface and is not safe to automate.
\item \code{"warn"}: warn and leave the token unchanged.
\item \code{"skip"}: silently leave the token unchanged.
}}
}
\value{
character(1), the rewritten source code. The original is returned
unchanged if no non-ASCII tokens are found.
}
\description{
Rewrite non-ASCII characters inside a string of R source code
}
\details{
This function does \strong{not} touch identifiers, even with
\code{identifiers = "skip"}: CRAN's policy is to forbid non-ASCII identifiers,
but rewriting them automatically is unsafe (it would silently rename the
user's exported API). Use \code{\link[=find_nonascii_tokens]{find_nonascii_tokens()}} to surface them.

Strings declared with the R 4.0 raw form (\code{r"(...)"}, \code{R"---(...)---"})
are detected - by default they are still treated like regular \code{STR_CONST}
(escaped); pass \code{strategy = "translit"} if you want to keep them raw and
lose the accents instead.
}
\examples{
src <- '
# accent dans un commentaire: ete
x <- "deja vu"
'
cat(asciify_r_source(src))
}
