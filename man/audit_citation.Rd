% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit_citation.R
\name{audit_citation}
\alias{audit_citation}
\title{Audit \code{inst/CITATION} for old-style calls flagged by CRAN}
\usage{
audit_citation(pkg = ".")
}
\arguments{
\item{pkg}{Path to the package to audit.}
}
\value{
A tibble with columns \code{call}, \code{line}, \code{suggestion}. Empty
when the file is modern, or when there is no \code{inst/CITATION}.
}
\description{
Surfaces every call to \code{personList()}, \code{as.personList()} or
\code{citEntry()} in the package's \code{inst/CITATION} file, with the line
number and a one-line suggestion of the modern equivalent. CRAN
rejects new submissions whose CITATION file still uses these
(\verb{Package CITATION file contains call(s) to old-style ...}).
}
\details{
Detection is purely static (parse + token walk via
\code{\link[utils:getParseData]{utils::getParseData()}}), so it does not source the file and never
executes user code.
}
\examples{
\dontrun{
audit_citation(".")
}
}
