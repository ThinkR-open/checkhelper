% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit_check.R
\name{audit_check}
\alias{audit_check}
\title{Run R CMD check with CRAN environment}
\usage{
audit_check(
  pkg = ".",
  check_output = file.path(dirname(normalizePath(pkg, mustWork = FALSE)), "check"),
  scratch = tempfile("scratch_dir"),
  Ncpus = 1,
  as_command = FALSE,
  clean_before = TRUE,
  open = FALSE,
  repos = getOption("repos")
)
}
\arguments{
\item{pkg}{Path to the package to check.}

\item{check_output}{Where to store check outputs.}

\item{scratch}{Where to store temporary files.}

\item{Ncpus}{Number of CPUs.}

\item{as_command}{Run as Linux command line instead of in R.}

\item{clean_before}{Wipe \code{check_output} before running.}

\item{open}{Open the check directory at the end.}

\item{repos}{Repositories used for dependency resolution.}
}
\value{
The \code{rcmdcheck} results object.
}
\description{
Invokes \verb{R CMD check} with the env vars and options used by CRAN's
incoming-pretest scripts, so local checks match CRAN as closely as
possible. Wraps \code{\link[=check_as_cran]{check_as_cran()}}.
}
\examples{
\dontrun{
audit_check(".")
}
}
\seealso{
\code{\link[=check_as_cran]{check_as_cran()}}.
}
