% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit_downloads.R
\name{audit_downloads}
\alias{audit_downloads}
\title{Audit calls to known download / network functions}
\usage{
audit_downloads(pkg = ".")
}
\arguments{
\item{pkg}{Path to the package to audit.}
}
\value{
A tibble with columns \code{file}, \code{line}, \code{function} and
\code{suggestion}. Empty when no download call is found, or when the
package has none of the watched directories.
}
\description{
CRAN policy: package code that downloads files or hits the network
at install / runtime must degrade gracefully when the network is
unavailable (offline build farms, sandboxed CI, locked-down user
environment). Common rejection causes: downloads from inside
\code{.onLoad()}, \code{.onAttach()}, vignettes or examples that have no
\code{tryCatch()} / \code{skip_if_offline()} / \verb{\dontrun{}} guard.
}
\details{
\code{audit_downloads()} walks \verb{R/}, \verb{tests/}, \verb{vignettes/} and \verb{inst/}
and surfaces every call to a known download or HTTP function so
the dev can review each one. Detection is purely static: each file
is parsed and the AST is walked; nothing is sourced.
}
\examples{
\dontrun{
audit_downloads(".")
}
}
